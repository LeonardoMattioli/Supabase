name: Pull Request

on: [pull_request]

jobs:
  login:
    name: Logando no Supabase
    runs-on: ubuntu-latest
    steps:
      - name: checkout
        uses: actions/checkout@v3

      - name: Enviroment Variable
      - run: npx supabase db push